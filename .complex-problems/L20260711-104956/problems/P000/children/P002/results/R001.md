# iOS 分页 reducer 与 KBLite CAS 结果

## Summary

已增加严格 KnowledgeChangePage/KnowledgeChangeFeedReducer 纯模型，以及 KBLite graph snapshot mutation token 与 expected-token/user CAS apply。

## Evidence

- 三页 reducer 仅终页输出最终权威 snapshot；legacy 单页兼容。
- revision gap、空中间页、target 漂移、页数上限均被 model smoke 拒绝。
- KBLite snapshot 使用 canonical encoded graph 的 FNV token，无需修改所有业务写入口；任何 graph 内容变化都会使 CAS stale。
- CAS 同时校验当前 user，切用户不能应用旧 snapshot。
- `run-knowledge-change-feed-pagination-model-smoke.sh` 通过。
- Simulator workspace Debug build 通过。

## Remaining Risk

- Coordinator 尚未消费 reducer/CAS，将在 P003 集成。
