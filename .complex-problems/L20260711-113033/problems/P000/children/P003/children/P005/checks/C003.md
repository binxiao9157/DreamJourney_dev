# 双仓提交推送与线上只读审计验收检查

## Summary

P005 成功。双仓功能提交已推送，后端生产容器运行最新版本，真实 Postgres 的 owner-bound aggregate audit 合同已有脱敏证据。

## Evidence

- 后端 origin/main 和服务器 HEAD 均包含 `dd88f17`，核心功能提交为 `f62e5a9`。
- iOS origin feature 分支包含 `fc5772d`。
- health 返回 production/postgres，API、Postgres、Redis 容器均正常。
- deployed smoke 返回 canonicalSourceRefCount=1、recommendedAction=none、crossAccountDenied=true、aggregateOnly=true。

## Criteria Map

- 双仓提交推送：完成。
- 服务器部署：完成并运行最新 HEAD。
- 线上 schema/聚合/权限/隐私：完成。
- 不迁移历史、不真机：遵守。

## Execution Map

- Git 提交提供可追踪版本。
- Docker Compose 重建 API 并保留服务器 `.env`。
- 独立 deployed smoke 通过临时用户与 canonical mutation 验证真实合同。

## Stress Test

- 首次部署 smoke 暴露线上 403 detail shape 与 TestClient 不同；脚本改为验证 403 和存在 detail。
- 第二次暴露 `counts.sourceRefs` 是合法聚合键而非来源列表；泄漏检查修正后仍禁止 graph、statement、实体 ID 和 session ID，最终 smoke 通过。

## Residual Risk

- 历史 legacy refs 仍只读审计，实际迁移需独立批准。
- 未执行真机验证，符合本任务边界。

## Result IDs

- R003
