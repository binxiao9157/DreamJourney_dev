# 后端 canonical 来源与只读审计结果

## 完成

- V2 proposal 从 validated `sessionId + sourceTurnIndices` 派生逐实体 `conversationTurn` refs。
- 请求级来源 refs 不再被信任；服务端已有 refs 原样保留，避免隐式历史迁移。
- 统一 canonical/legacy source kind 与脱敏 title；现有 mailbox/memoir/import 等合法来源不会误判 unknown。
- 新增纯 source-ref audit 与 owner-path endpoint，只返回 revision、分类计数和建议，不含 graph、正文、source ID/title。
- endpoint 在生产 shadow 模式下也通过 principal-bound ownership 拒绝跨用户访问。
- 没有 apply migration，没有修改历史 snapshot。

## 验证

- 针对性 19 tests 通过。
- `scripts/verify_backend.sh`：259 tests、compile、FastAPI 与知识 smoke 全部通过。
- `git diff --check` 通过。

## 边界

- iOS canonical 生成和 typed consumer 由 P002 完成。
- 线上部署和只读 audit smoke 由 P003 完成。
