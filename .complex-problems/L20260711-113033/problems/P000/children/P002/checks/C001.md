# iOS canonical 来源生成与 typed audit consumer 验收

## Summary

P002 已满足成功条件。新文本与照片来源均由稳定 canonical 规则生成，typed audit consumer 严格校验聚合响应，且没有自动迁移历史来源。

## Evidence

- 模型 smoke 覆盖 canonical/legacy/unknown 分类、turn 去重、照片资源 ID 校验与 audit 响应校验。
- client 静态检查确认新 endpoint、source contract 与禁止旧 ref 生成。
- DreamJourney Debug iOS Simulator generic target 完整构建成功。
- `git diff --check` 通过。

## Criteria Map

- 新文本 canonical：`conversationTurn + session-{sessionId}:turn-{turnIndex}` 已实现并验证。
- 新照片 canonical：`conversationPhoto + photo-{stableAssetId}` 已实现并验证，不使用本地绝对路径。
- 无自动迁移：仅聚合审计 legacy refs，加载和写入路径均未增加历史重写。
- 模型检查通过：专用 Swift smoke 与真实 app target 编译均通过。

## Execution Map

- `KBLiteModels.swift` 定义 canonical policy 和 strict typed audit model。
- `KBLiteManager.swift` 应用到文本、照片和来源合并。
- `AIRecordingViewController.swift` 提供稳定照片资源 ID。
- `DreamJourneyBackendClient.swift` 消费只读审计 endpoint 并声明合同版本。

## Stress Test

- 非法照片 ID、重复 turn index、legacy/unknown refs、计数不一致和用户不一致响应均被测试覆盖或拒绝。
- 真实 Swift target 曾暴露类型检查超时，拆分表达式后完整构建通过。

## Residual Risk

- 历史 legacy refs 尚未迁移，这是明确的产品安全边界，不阻塞本问题；未来迁移必须另行审计并获得批准。
- 跨仓发布接线与线上部署属于 P003，不属于 P002 成功条件。

## Result IDs

- R001
