# Session Lease 第一阶段结果

## Summary

已完成后端内存/Postgres lease store 与原子容量仲裁，但 API 合同、iOS 消费和组合验证尚未完成，因此父任务仅为部分结果。

## Done

- 完成 R000：store schema、acquire/reuse/replace/conflict、heartbeat、release、TTL 和安全持久化。

## Verification

- `tests.test_postgres_store` 通过。
- Postgres advisory lock 与 credential 不落库断言通过。

## Known Gaps

- `/digital-human/sessions` v2、heartbeat/release 路由和 runtime capability 未实现。
- iOS session lease 解析、heartbeat、生命周期 release 和旧响应清理未实现。
- 后端/iOS 组合 smoke、构建和 release regression 未执行。

## Artifacts

- R000
