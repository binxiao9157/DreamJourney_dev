# Session Lease API、iOS 消费与组合验收成功检查

## 结论

`R002` 足以解决 `P002` 定义的非真机开发问题，判定为成功。

## 逐项核对

- 后端存储：内存与 Postgres 都实现 lease 保存、复用、同设备上下文替换、TTL 过期与幂等释放。
- 并发边界：Postgres 使用 advisory transaction lock，第二设备超出容量时返回结构化 `409`，释放或过期后可重新获取。
- 安全边界：持久化 payload 不包含腾讯 appkey、accesstoken 或 credential，相关测试已覆盖。
- API 合同：session v2、runtime capability、heartbeat、release 均有 API 与回归测试。
- iOS 消费：已解析 runtime/lease，执行定时 heartbeat，统一生命周期 release，并释放 stale success response。
- 产品语义：用户停止当前对话不释放腾讯 runtime/lease；页面退出、角色切换、后台宽限期到期和 provider 终止路径会释放。
- QA 污染：runtime stub smoke 会在完成后 heartbeat 并 release，不再长期占用唯一并发。
- 自动化：专项测试、全量后端验证、iOS build、静态守卫、模拟器组合 gate 与 diff check 全部通过。

## 严格性说明

- 真实腾讯配额释放速度、真实渲染、音频和麦克风行为未被本结果证明，但这些内容在任务 Out Of Scope 中明确属于后续真机/线上验收，不构成本问题失败。
- 后端未部署，因此不能宣称线上合同已经生效；当前结果只证明代码、存储适配和非真机闭环已完成。
