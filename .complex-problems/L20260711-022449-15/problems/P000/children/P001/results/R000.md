# 后端结构化证据提取完成

## Summary

`/kb/extract` 已 additive 支持 extraction v2 structured turns 和 `userEvidenceOnly`，provider 输出必须引用有效 user turn；普通响应返回无正文 evidencePolicy，v1 transcript 保持兼容。

## Done

- 新增输入规范化、turn 数量/单轮/总长度校验、证据后过滤和统计 helper。
- DeepSeek prompt 明确 assistant 只能帮助理解，不能作为事实来源。
- endpoint dryRun/正式响应兼容 v1/v2，并从 context 删除 turns/transcript/summary。
- 新增 endpoint/helper 单测、evidence smoke 和 verify_backend 接入。

## Verification

- 子任务运行 202 项 unittest 全部通过，knowledge delta/v2/evidence smoke 通过。
- 主控复跑 evidence helper 两项定向单测和 memory smoke 通过。
- Python/shell 基础检查和 `git diff --check` 通过。

## Known Gaps

- 未调用真实 DeepSeek provider；当前通过 mock 验证返回过滤合同。
- legacy v1 按兼容要求维持旧语义，iOS 新调用固定使用 v2。
- dryRun 继续返回调试 prompt，调用方不得把 dryRun 响应作为普通日志或用户响应。

## Artifacts

- `app/services/knowledge_extraction.py`
- `scripts/backend-knowledge-evidence-smoke.py`
- `scripts/run-backend-knowledge-evidence-smoke.sh`
