# iOS 证据与 Persona-bound Context 完成

## Summary

iOS 已使用 structured user evidence 合同、独立后端精提取水位和统一 fact 生成门禁；Echo Context gate 绑定 user/persona/digital-human identity，family 失败不再读取 viewer 私有 KBLite。

## Done

- v2 indexed turns 包含 user/assistant，legacy transcript 仅包含 user 证据。
- optional backend extraction session/date 水位兼容旧图谱，只有后端成功响应推进。
- high/confirmed + generationAllowed 事实才能进入直接或人物关联的本地生成文本。
- Context packet 从 `persona` 子对象解析身份并兼容顶层字段；family canonical ID 优先 repository digitalHumanId。
- timeout/backend missing/failure/contract missing/identity mismatch 统一经过 persona fallback policy；family fail closed。
- 空后端/本地上下文明确结束 gate，不残留 timeout/retry。
- 新纯 Swift model smoke、静态 guard 和 Xcode target 接入完成。

## Verification

- policy model smoke、evidence/context static guard、knowledge pipeline guard、three-way merge model smoke 通过。
- DreamJourney workspace generic iOS Simulator Debug build 通过。
- `project.pbxproj` 包含两个新增 policy source；`git diff --check` 通过。

## Known Gaps

- 未做真机 Echo 语义/声音验收，符合本任务约束。
- family persona KBLite 本地 fallback 在 persona-scoped knowledge schema 完成前保持关闭。

## Artifacts

- `DreamJourney/Sources/Services/KnowledgeGenerationPolicy.swift`
- `DreamJourney/Sources/Services/EchoKnowledgeContextPolicy.swift`
- `Scripts/QA/prd-stitch-ui/knowledge-context-policy-model-smoke.swift`
- `/tmp/task14-ios-simulator-build.log`
