# 后端 Context P0 门禁成功检查

## Summary

P002 的低置信事实、persona、时间信件和 care viewer 边界均有代码与负向自动化证据，且旧 Context V2 合同保持兼容。

## Evidence

- low/familyCircle fact reason 与 generation absence 单测、smoke 通过。
- family persona personal fact 不注入单测通过。
- recipient mismatch、recipient not due、owner not due 单测通过。
- owner care 不替代 viewer care 和 accepted viewer 专属 care 单测通过。

## Criteria Map

- high/confirmed 门禁：候选、memory 摘要和 generation renderer 三层一致过滤。
- 时间信件：draft、recipient、openAt 均在 archive candidate 前处理。
- care viewer：viewer-specific store read 且无 owner fallback。
- trace/hash：既有重复请求和 Context V2 smoke 继续通过。

## Execution Map

- 结果 R001 覆盖 builder、fixtures、单测和 smoke。

## Stress Test

- 同一 snapshot 同时包含 high、confirmed、low 和 familyCircle fact；同一封信分别以正确收件人、错误收件人和本人请求。

## Residual Risk

- family persona KBLite 暂时减少召回，这是缺少 persona-scoped entity schema 时的安全取舍，不是 fail-open。

## Result IDs

- R001
