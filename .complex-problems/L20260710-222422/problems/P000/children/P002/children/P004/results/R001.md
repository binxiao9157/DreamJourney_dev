# 后端 Principal 强绑定结果

## Summary

全路由注册表已接入授权中间件；普通用户的 owner mismatch 与 system-only 调用现在形成即时安全边界，同时保持全局模式为 shadow。

## Done

- `AuthorizationPolicyDecision` 新增 principal-bound 标记。
- ownerBody/ownerPath 由注册表提取 owner 并与 access token principal 比较。
- systemOnly 对 user principal 即时拒绝，legacy backend token 保持兼容。
- delegated family、timeLetter、invitation、care 策略优先于通用 owner 规则。
- 大 JSON 请求不再跳过 ownership 检查，消除 256 KB 以上 body 绕过路径。
- runtime config 暴露 54 路由、0 未分类的无 PII 审计摘要，`productionEnforceReady` 仍为 false。

## Verification

- `STORE_BACKEND=memory PYTHONPATH=. .venv/bin/python -m unittest discover -s tests`
- 结果：177 tests passed。
- 参数化负向覆盖 profile、archive、mailbox、voice、KB、memory、digital-human session、push、echo、family。
- 正向覆盖 owner profile、family/care/timeLetter delegated、legacy backend token system dispatch。

## Known Gaps

- 尚未运行部署环境 Postgres smoke；由 P003 统一提交、部署并生成线上证据。
- iOS mailbox 仍需移除主动 dispatch 调用；由 P005 处理。

## Artifacts

- `/Users/yxj/Documents/Codex/Video/DreamJourneyBackend/app/services/authorization_policy.py`
- `/Users/yxj/Documents/Codex/Video/DreamJourneyBackend/app/services/route_ownership.py`
- `/Users/yxj/Documents/Codex/Video/DreamJourneyBackend/app/main.py`
- `/Users/yxj/Documents/Codex/Video/DreamJourneyBackend/tests/test_auth_sessions.py`
- `/Users/yxj/Documents/Codex/Video/DreamJourneyBackend/tests/test_authorization_policy.py`
