# iOS 时间信件调度职责收敛成功检查

## Summary

R002 满足 P005 的全部成功标准。公开 App 不再要求 system-only 调度能力，且现有 mailbox 提醒体验通过模拟器 smoke。

## Evidence

- repository 中不存在 `DreamJourneyBackendClient.shared.dispatchDueTimeLetters`。
- 时间信件静态检查、archive snapshot 检查和 simulator reminder smoke 均通过。
- smoke 证明多封提醒、详情解析、已读、归档和 sibling unread 状态未回归。

## Criteria Map

- 只拉取 mailbox：代码与禁止调用静态断言覆盖。
- backend client system API 保留：delivery shell check 继续断言合同存在。
- 提醒行为不回归：simulator smoke JSON 全部为通过状态。

## Execution Map

- 生产代码、静态 guard、状态文档和模拟器 smoke 证据均记录在 R002。

## Stress Test

- 可信失败模式为移除 dispatch 后提醒列表不刷新；smoke 通过 stub mailbox 返回两封提醒并验证详情、已读和归档，证明读取链路独立成立。

## Residual Risk

- 服务器 timer 停止将影响到期投递，需要部署监控承担；不应由普通客户端恢复 system 权限。

## Result IDs

- R002
