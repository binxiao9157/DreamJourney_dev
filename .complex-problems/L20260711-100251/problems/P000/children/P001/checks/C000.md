# 后端 operation receipt 成功检查

## Verdict

成功。结果覆盖了原问题要求的稳定指纹、Memory/Postgres receipt、四类 API、Archive 无级联删除、governance 构建前重放、legacy 兼容和 purge；相关测试与 diff 检查有可重复证据。

## Criteria Review

- 不同 payload/schema/kind/item：由 receipt kind/hash 比较拒绝，API 映射稳定 409。
- 相同 payload、不同 base revision：baseRevision 不进入 fingerprint，receipt 在 revision 校验前重放。
- 事务性：Postgres receipt 在同一 connection/transaction 中随 snapshot/change/archive 提交，异常统一 rollback。
- legacy：无 receipt 的历史 kb_changes 返回 duplicate 且 `operationPayloadVerified=false`。
- 隐私：响应不含 hash 或请求正文，仅含 verified 布尔值与 operation ID。

## Evidence Quality

相关 204 项测试在主控环境再次通过，worker 全量 250 项验证通过；新增 API detail 字段后断言已更新并通过。

## Residual Risk

线上 Postgres migration 与部署 smoke 属于当前任务明确排除的外部验收边界，将在后续部署阶段执行。
