# 收敛 Task 17 跨仓交付

## Problem Definition

后端 receipt 与 iOS recovery 已分别实现，但还需要进入长期 QA 门禁、canonical 知识架构与状态文档，并完成两个仓库的最终非真机验证和独立提交。

## Proposed Solution

将 operation receipt/payload conflict 场景纳入治理 gate 和 release regression，更新产品知识库架构、Task 17 状态和 QA 命令；运行后端全量验证、release regression、Simulator 与 generic iPhoneOS build，清理临时产物后分别提交。

## Acceptance Criteria

- Release QA 始终执行 outbox recovery model/client/coordinator checks。
- Canonical design 明确 receipt、legacy、隐私和客户端恢复语义。
- 后端全量 250 项测试、跨仓 gate、release regression 和两类非真机构建通过。
- 两仓 git diff --check 通过并独立提交，工作树仅保留明确忽略产物。

## Verification Plan

运行 `scripts/verify_backend.sh`、`run-knowledge-governance-gate.sh`、`run-release-regression.sh`、Simulator workspace build 和 `run-iphoneos-generic-build.sh`，记录报告路径与 commit。

## Risks

- Release regression 较长，需区分环境噪声与代码回归。
- Closure 自动同步可能在 progress.md 末尾产生空白，需要最终 diff 检查修正。

## Assumptions

- 本轮不推送、不部署、不运行真机。
