# Widget 读取与工程接线均已完成

## Summary

P003 的两个拆分子问题均已成功关闭：Widget 读取端严格校验 schema/账号并最小展示，Xcode 工程已嵌入扩展并统一 App Group/Bundle/Team 配置。

## Done

- P005/R002：schema v2 reader、fail-closed provider、无 description entry/view、privacySensitive 和模型 smoke。
- P006/R003：target membership、Embed App Extensions、target dependency、双方 entitlement/Info/build settings 和产物构建验证。

## Verification

- reader 模型 smoke 通过，旧 schema/身份异常/损坏/非法字段均为空。
- Simulator 与 generic iPhoneOS build 通过。
- 两种 App 包均嵌入 `DreamJourneyWidget.appex`；本地 Bundle ID 覆盖后扩展正确派生 `.widget`。

## Known Gaps

- Apple Developer Portal App Group 注册、带签名 Archive 和真机 Widget Gallery/锁屏脱敏仍是明确外部验收项。

## Artifacts

- Results R002、R003
- Checks C002、C003
