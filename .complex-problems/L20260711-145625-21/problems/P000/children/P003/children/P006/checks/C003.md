# Widget 工程交付合同验收通过

## Summary

R003 满足 P006 的 target membership、扩展嵌入、配置继承与非真机构建要求，外部 provisioning 风险已明确保留。

## Evidence

- Target graph 实际显示 DreamJourney 显式依赖 DreamJourneyWidget。
- Simulator 与 iPhoneOS 两种 SDK 构建均成功。
- 构建产物中的 `.appex`、主/扩展 bundle IDs 和 App Group Info 值已直接读取验证。

## Criteria Map

- 新文件 membership：两类构建编译成功证明。
- 嵌入/dependency：target graph 与 App/PlugIns 产物证明。
- 同一 App Group/entitlements：源 plist lint、showBuildSettings 与构建 Info 值证明。
- Bundle/team 同源：本地 override 下 showBuildSettings 和产物 bundle ID 证明。
- 不含私密配置：只新增 build setting 占位和 entitlement，没有修改 LocalConfig。

## Execution Map

- T005/R003 覆盖所有 P006 工程产物；首轮构建暴露 phase cycle，已在同一执行中修复并重新构建通过。

## Stress Test

- 同时使用本地 Bundle ID/Team 命令行覆盖构建主 App 和扩展，验证扩展不是固定到其他同事的 bundle/team。
- 对 Simulator 与真实 iPhoneOS SDK 分别构建，避免只在一种平台偶然通过。

## Residual Risk

- App Group capability 和 provisioning profile 不能由无签名 generic build 证明，必须保留为后续真机外部门槛。

## Result IDs

- R003
