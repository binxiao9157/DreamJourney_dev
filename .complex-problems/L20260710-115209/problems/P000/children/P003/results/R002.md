# iOS、QA smoke 与文档已对齐 v2 音色合同

## Summary

iOS 新建 profile 已使用 `vp_` 逻辑 ID并解析 provider binding 状态；deployed smoke 改为按真实 owner refresh/quality-accept/synthesis，且不再删除线上音色。状态、部署和调用链文档已更新。

## Done

- `VoiceCloneService.makeSpeakerId()` 改为 `vp_...`，旧 `S_` 持久化值仍可读取。
- iOS contract/snapshot 新增 binding mode、managed 和 slot state。
- 修复家人音色 ready 但 disabled 时状态文案误报可用的问题。
- deployed smoke 强制 `VOICE_CLONE_READY_PROFILE_USER_ID`，不创建/删除真实 profile。
- 静态 guard 覆盖逻辑 ID、profile owner、不删除音色、无哈希分配、Postgres 锁和容量错误。
- 更新当前实现、数字人/音色路由、部署 smoke、服务器运维和独占槽状态文档。

## Verification

- 9 个声音复刻/家人/角色 Swift 静态检查全部通过。
- release QA package check 通过；仅提示历史截图证据目录未完整，不影响本次合同检查。
- iOS Debug simulator generic build 通过，使用 `com.yxj.dreamjourney.app` / `2BTR77V3R8` 命令行覆盖。
- 后端 `verify_backend.sh` 通过 140 tests 和全部 smoke。

## Known Gaps

- 未部署后端，线上 `/config/runtime` 和 Postgres schema 尚未验证 v2。
- 未运行真机听感；本任务明确不包含真机。
- 现有 UIKit 文件仍有若干 `contentEdgeInsets` deprecated warning，与本合同无关。

## Artifacts

- `DreamJourney/Sources/Memoir/VoiceCloneService.swift`
- `DreamJourney/Sources/Services/DreamJourneyBackendClient.swift`
- `Scripts/QA/prd-stitch-ui/backend-voice-clone-deployed-smoke.py`
- `docs/superpowers/status/2026-07-10-voice-clone-exclusive-slot-allocation.md`
