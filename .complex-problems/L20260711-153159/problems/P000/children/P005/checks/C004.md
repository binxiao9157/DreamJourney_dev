# Task 22 发布回归与文档收口复核

## Summary

回归与文档证据完整，但独立复核证明家庭 authority 新鲜度和异步读取边界仍未完全闭合，因此当前结果不能判定成功。

## Blocking Gaps

- App 前台恢复不会先刷新家庭关系；服务端已撤销成员在进程存活期间仍可能保留于 account sync authorization snapshot。
- extraction/governance 后台路径仍可能通过当前 context 间接读取可变 FamilyRepository，账号切换或撤销时存在竞态窗口。
- 当前选中的 family context 失效后没有主动持久化回 self 并广播 context change，Echo UI/runtime 可能短暂保留旧角色。

## Result IDs

- R004
