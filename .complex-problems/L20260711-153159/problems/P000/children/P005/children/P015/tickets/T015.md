# 更新 Task 22 最终状态与验收证据

## Problem Definition

最终代码和 final8 回归已完成，但 canonical/status/matrix/index 仍停在初始 Task 22 或 final5，无法作为后续唯一权威口径。

## Proposed Solution

更新 Task 22 状态文档、canonical 知识架构、PRD coverage、release matrix、impl plan index 与 task plan，记录 freshness/snapshot/reconciliation/refresh token/authorization epoch 合同、全部新增 gates、final8 路径和无后端部署影响。

## Acceptance Criteria

- 文档明确 Task 22 非真机实现完成、待提交，而非 active 开发中。
- final8 report 和新增验证命令可直接找到。
- coverage/release/canonical 对家庭 authority 和后续非目标表述一致。
- 文档矩阵 gates、release QA、Closure validate/check 与 `git diff --check` 通过。

## Verification Plan

运行 release feature matrix、PRD coverage、release QA package、`git diff --check`、Closure sync/check；提交前再次检查 status/index。

## Risks

- 提交哈希只能在提交后补写；提交前使用“等待最终提交”，提交后追加 delivery commit。

## Assumptions

- 本轮后端仓库 clean，Task 22 无需部署。
