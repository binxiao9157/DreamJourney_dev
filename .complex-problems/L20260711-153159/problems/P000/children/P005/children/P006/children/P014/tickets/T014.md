# 更新统一知识管线静态发布合同

## Problem Definition

统一知识管线 gate 仍要求 Task 12 时代的 extraction 日志与 foreground 直接同步，和 Task 22 的授权 snapshot、family refresh-first 行为冲突。

## Proposed Solution

只更新 `knowledge-pipeline-check.swift` 的过期断言：KBLite 用户隔离改为验证 `KBPersonaAuthorizationSnapshot`/锁内 generation，foreground 改为验证 family bootstrap completion 后同步；保留后端优先提取、隐私、三方合并、稳定分页、Widget 和 Echo RAG 原有检查。

## Acceptance Criteria

- knowledge pipeline gate 通过且不重新接受 mutable persona resolver。
- release QA package 通过。
- full release regression 包含 Simulator、generic iPhoneOS 和 delayed reply smoke 并成功。
- `git diff --check` 通过。

## Verification Plan

先单跑 knowledge pipeline gate；根据实际失败逐项确认是过期合同还是运行时缺口；之后重跑 final7 全量 regression。

## Risks

- 不允许删除与本任务无关的原有断言，只能替换已有安全合同已明确淘汰的部分。

## Assumptions

- final6 前置 Task 22 gates 均通过，失败点仅为旧 pipeline gate。
