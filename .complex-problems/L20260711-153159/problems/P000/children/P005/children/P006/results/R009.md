# 运行期家庭授权新鲜度与异步快照阶段结果

## Summary

P007/P008/P009 已分别完成前台刷新顺序、不可变 extraction/governance snapshot 和 Echo 主动回退；独立并发复核仍发现两个必须继续修复的排序缺口。

## Done

- foreground 在家庭 authority 刷新完成后才启动知识同步。
- extraction 使用 user/persona/family generation snapshot，后台不读取 FamilyRepository/context。
- governance dispatch/success 使用 queue-owned account authorization 与 persona。
- family 授权失败或撤销后，持久化 Context 主动回退本人并通知 Echo。
- 新增 freshness、async snapshot、context reconciliation 模型/静态 gates。

## Verification

- P007、P008、P009 专项模型和静态 gates 通过。
- release QA package 通过。
- Workspace Simulator build 通过，最新日志 `/tmp/dj-task22-p009-build.log`。
- 独立 agent 完成并发时序复核。

## Known Gaps

- 同账号并发家庭 refresh 仅比较 user generation，旧 refresh 响应仍可能覆盖新成功/失败结果。
- persona/family invalidate 使用 `queue.async`；已经排入 coordinator 的旧治理 callback 可能先于失效块修改 graph/base/outbox。
- 全量 release regression 和 generic iPhoneOS build 尚待并发缺口修复后执行。

## Artifacts

- P007/R005、P008/R006+R007、P009/R008。
- `Scripts/QA/prd-stitch-ui/knowledge-async-authorization-snapshot-check.swift`
- `Scripts/QA/prd-stitch-ui/family-context-reconciliation-check.swift`
