# 家庭授权刷新状态与 foreground 顺序复核

## Summary

R005 满足 P007：授权状态、失败失效、foreground 顺序和同步恢复均有代码与专项验证。

## Evidence

- `FamilyAuthorizationFreshness` 模型 smoke 覆盖 generation、成功、刷新中、失败和 reset。
- FamilyRepository lifecycle gate 覆盖 refresh started/did refresh/failed 与 owner-bound snapshot。
- workspace Simulator build 和 release QA package 通过。

## Criteria Map

- ready-only knowledge snapshot：`acceptedMembersForKnowledgeSync` + freshness model。
- 失败/非法/账号切换失效：`invalidateAuthorization`、`reset`、generation 变化。
- foreground 顺序：SceneDelegate completion 后调用 `foregroundAfterFamilyRefresh`。
- stale response：既有 userGeneration/capturedGeneration guard 保留。

## Execution Map

- R005 完成模型、repository、coordinator、Scene 与 QA 接线。

## Stress Test

- 模型执行 ready -> refreshing -> failed，证明旧 verified snapshot 不再用于新 family sync；refresh start 会使旧异步 generation 失效。

## Residual Risk

- extraction/governance 和 Echo context 的后续闭环已分别由 P008/P009 显式承接，不阻断 P007 自身成功。

## Result IDs

- R005
