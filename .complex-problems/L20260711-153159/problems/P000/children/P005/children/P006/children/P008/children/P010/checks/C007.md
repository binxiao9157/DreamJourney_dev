# 既有知识发布门快照合同对齐复核

## Summary

R007 完整解决 P010：旧 gate 已验证新的安全合同，专项检查和 release QA package 均通过，未放宽原有业务约束。

## Evidence

- proposal/persona gate 从 resolver 字符串断言升级为函数体级 snapshot 断言。
- proposal/persona、async snapshot、governance coordinator 和 release QA package 全部通过。
- P008 Simulator build 已证明该合同对应的运行时代码可编译。

## Criteria Map

- 不再要求旧 resolver：满足。
- 明确捕获和传递 snapshot：满足。
- 完成阶段禁止 mutable authority 读取：满足。
- 保留 proposal/evidence/family fallback：满足。

## Execution Map

- R007 仅修改既有静态 gate，没有改变运行时或公开行为。

## Stress Test

- gate 以 brace depth 提取 `finishExtraction`，不会因相邻函数仍有合法 resolver 使用而误判。

## Residual Risk

- 最终全量回归在 P009 完成后一并执行，不阻断本验证合同自身成功。

## Result IDs

- R007
