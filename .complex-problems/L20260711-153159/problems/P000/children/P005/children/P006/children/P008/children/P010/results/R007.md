# 既有知识发布门快照合同对齐结果

## Summary

旧 proposal/persona gate 已改为验证不可变授权快照，不再要求后台完成阶段重读当前 persona。

## Done

- 新增 brace-depth 函数体提取，确保禁止项只检查 `finishExtraction`。
- 要求 ConversationMemory 在后台派发前捕获 `KBPersonaAuthorizationSnapshot`。
- 要求 KBLite 完成阶段只比较 generation snapshot，并禁止读取 `FamilyRepository`/当前 persona resolver。
- 保留 proposal-first、evidence、Echo family fallback 既有断言。

## Verification

- proposal/persona gate：通过。
- async authorization snapshot gate：通过。
- governance coordinator gate：通过。
- release QA package：通过。
- P008 Workspace Simulator build：通过。

## Known Gaps

- P009 Echo context 主动回退和最终全量 release regression 尚未执行。

## Artifacts

- `Scripts/QA/prd-stitch-ui/knowledge-proposal-persona-policy-check.swift`
