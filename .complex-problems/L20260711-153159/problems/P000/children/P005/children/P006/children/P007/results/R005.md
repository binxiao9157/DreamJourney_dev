# 家庭授权刷新状态与 foreground 顺序结果

## Summary

已引入 owner-bound 家庭授权新鲜度状态机，并把 foreground 知识同步改为等待后端家庭刷新完成；刷新开始、成功和失败均有明确的同步 generation 行为。

## Done

- `FamilyAuthorizationFreshness` 覆盖 unknown/refreshing/ready/failed、verified snapshot 与 generation。
- FamilyRepository 只有 verified snapshot 才向知识同步提供 family persona；刷新失败使 family 权限 fail closed。
- 知识同步在刷新开始时取消旧 generation；成功恢复 account authorization，授权集合变化才清 base；失败回到 personal-only 并重拉。
- Scene foreground 先刷新家庭关系，再触发 `foregroundAfterFamilyRefresh` 同步。
- 新增 freshness model smoke 并接入 release regression/release QA。

## Verification

- `run-family-authorization-freshness-model-smoke.sh` 通过。
- family repository 与 family sync/import 静态 gate 通过。
- release QA package 与 `git diff --check` 通过。
- DreamJourney workspace Simulator Debug build 通过，日志 `/tmp/dj-task22-p007-build2.log`。

## Known Gaps

- extraction/governance 的不可变 generation 校验由 P008 收口。
- Echo context 主动回退的完整 gate 由 P009 收口。

## Artifacts

- `DreamJourney/Sources/Services/FamilyRelationshipAuthorizationPolicy.swift`
- `DreamJourney/Sources/Services/FamilyRepository.swift`
- `DreamJourney/Sources/Services/KnowledgeSyncCoordinator.swift`
- `DreamJourney/Sources/SceneDelegate.swift`
- `Scripts/QA/prd-stitch-ui/family-authorization-freshness-model-smoke.swift`
