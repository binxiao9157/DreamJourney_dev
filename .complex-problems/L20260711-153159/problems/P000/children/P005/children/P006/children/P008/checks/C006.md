# Extraction 与 Governance 不可变授权快照复核

## Summary

R006 的运行时代码和专项新 gate 满足不可变快照方向，Simulator 编译通过；但既有 proposal/persona 发布门仍断言旧的后台重读实现并失败，因此当前尚不能判定 P008 成功。

## Evidence

- 新 snapshot 模型测试覆盖 user/persona/family generation 变化并通过。
- `finishExtraction`、`startNextGovernance` 和 `handleGovernanceSuccess` 已无 `FamilyRepository` 或当前 Context 重读。
- Workspace Debug Simulator build 成功。
- `run-knowledge-proposal-persona-policy-check.sh` 失败，仍要求 `resolveCurrentPersonaIdentity()` 旧实现。

## Criteria Map

- 开始前捕获不可变授权：已满足。
- 回调只比较 token：已满足。
- governance 使用 queue-owned snapshot：已满足。
- 所有现有发布门接受新合同：未满足。

## Execution Map

- R006 完成运行时代码、新模型和新静态 gate；既有 proposal/persona gate 未同步到新安全合同。

## Stress Test

- 用不同 user/persona/family generation 执行模型 smoke，旧 snapshot 均被拒绝。
- 运行既有 proposal/persona gate，确认发布回归仍会被旧断言阻断。

## Residual Risk

- 在旧 gate 更新并重新运行前，全量 release regression 不能作为 P008 完成证据。

## Blocking Gaps

- 更新既有 proposal/persona 静态检查，使其明确要求主线程授权快照，并禁止完成回调重新读取当前角色。

## Result IDs

- R006
