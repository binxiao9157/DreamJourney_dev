# 同账号家庭刷新响应代次隔离复核

## Summary

R010 满足 P012：最新 refresh 是唯一能提交 authority 的请求，旧成功、旧失败和重复 callback 都因 refresh generation 不匹配而拒绝。

## Evidence

- 纯 policy 同时验证 owner、user generation 和 refresh generation。
- 模型实际执行 refresh A -> refresh B -> B commit，并证明 A 与重复 B 均 stale。
- repository lifecycle gate 要求捕获和消费 refresh generation。

## Criteria Map

- A/B 乱序：满足。
- 账号/owner 切换：policy 同时比较两者，满足。
- 不执行旧 replace/invalidate：guard 位于 result switch 之前，满足。

## Execution Map

- R010 完成 policy、repository 接入和回归。

## Stress Test

- A token、B token和 B commit 后 token 三阶段均有模型断言。

## Residual Risk

- coordinator callback 失效线性化由 P013 独立处理。

## Result IDs

- R010
