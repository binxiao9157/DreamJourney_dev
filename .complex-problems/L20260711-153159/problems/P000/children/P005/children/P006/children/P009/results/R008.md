# 家庭授权撤销后的 Echo Context 主动回退结果

## Summary

失效 family context 现在会在主线程持久化回本人 Context，并通过现有通知链同步重建 Echo、KBLite 和治理 persona；正常 accepted family 不会被误重置。

## Done

- `acceptedMember(by:)` 增加 freshness guard，授权刷新失败后不再返回旧成员。
- 新增纯 `FamilyContextReconciliationPolicy`，明确 self/accepted/revoked/signed-out 行为。
- ContextStore 读取原始 owner-scoped 持久化 Context，只有失效 family 才写回本人。
- Context setter 更新 KBLite persona generation 和 coordinator queue persona，并对相同 Context 去重通知。
- 新增 model/static smoke 并接入默认 release regression 与 release QA package。

## Verification

- family context reconciliation model/static gate：通过。
- family repository、Echo family authorization、async snapshot gate：通过。
- release QA package：通过。
- Workspace Simulator build：通过，日志 `/tmp/dj-task22-p009-build.log`。

## Known Gaps

- 独立复核发现同账号并发 refresh 乱序和 coordinator 异步失效排序两个 P0 竞态，需由 P006 后续问题收口。
- 全量 release regression 与 generic iPhoneOS build 在上述竞态修复后执行。

## Artifacts

- `DreamJourney/Sources/Services/FamilyRelationshipAuthorizationPolicy.swift`
- `DreamJourney/Sources/Services/FamilyRepository.swift`
- `DreamJourney/Sources/App/DigitalHumanContextStore.swift`
- `Scripts/QA/prd-stitch-ui/family-context-reconciliation-model-smoke.swift`
- `Scripts/QA/prd-stitch-ui/family-context-reconciliation-check.swift`
