# Extraction 与 Governance 不可变授权快照最终复核

## Summary

R006 与 R007 共同满足 P008：运行时使用不可变 generation snapshot，旧发布门也已对齐并通过，后台提取/治理不再读取可变家庭仓库或当前 Context。

## Evidence

- generation 模型覆盖用户、角色、家庭授权三种失效并通过。
- 函数体静态 gate 证明 `finishExtraction`、`startNextGovernance`、`handleGovernanceSuccess` 无 mutable authority 重读。
- proposal/persona、governance coordinator、family freshness、release QA package 均通过。
- Workspace Simulator build 成功，日志 `/tmp/dj-task22-p008-build2.log`。

## Criteria Map

- 开始前捕获 identity/user/persona/family generation：满足。
- 完成回调只比较锁内 token：满足。
- governance queue 使用 account authorization + active persona：满足。
- stale callback 不写 graph/base/outbox：账号、角色或授权变化都会旋转 sync/extraction generation，旧回调在写入前返回。

## Execution Map

- R006 实现运行时、模型和新 gate。
- R007 修正既有发布门，恢复完整验证链路。

## Stress Test

- 模型分别替换 user/persona/family generation，旧 snapshot 均被拒绝。
- 角色变化使 coordinator `syncGeneration` 失效，治理 callback 的 `isCurrent` guard 在处理响应前返回。

## Residual Risk

- P009 仍需证明家庭授权撤销会主动持久化 self context 并通知 Echo；该问题独立于 P008 的后台快照闭环。

## Result IDs

- R006
- R007
