# Coordinator 授权失效线性化结果

## Summary

coordinator 现以锁保护 authorization epoch 作为 queue generation 的外部失效令牌；账号、角色和家庭刷新开始会在 queue invalidation 前轮换，旧 callback 的 `isCurrent` 无法继续写入。

## Done

- 新增纯 `KnowledgeAuthorizationEpochState` 与 rotate/accept 模型。
- coordinator 增加 epoch lock、owner/persona metadata 与 queue-specific 同步执行。
- user/persona/family refresh invalidation 轮换 epoch，并在返回前更新 sync generation/active epoch。
- `isCurrent` 同时验证 active user、sync generation 与 authorization epoch；`isCurrentPull` 继承该约束。
- 新增 coordinator epoch static gate 并接入 release regression/release QA。

## Verification

- knowledge context policy epoch model：通过。
- coordinator epoch、governance coordinator、async snapshot gates：通过。
- release QA package：通过。
- Workspace Simulator build：通过，日志 `/tmp/dj-task22-p013-build2.log`。

## Known Gaps

- 等待独立并发复核确认上次两个 P0 均已关闭。
- 全量 release regression 与 generic iPhoneOS build 由父问题统一执行。

## Artifacts

- `DreamJourney/Sources/Services/EchoKnowledgeContextPolicy.swift`
- `DreamJourney/Sources/Services/KnowledgeSyncCoordinator.swift`
- `Scripts/QA/prd-stitch-ui/knowledge-coordinator-authorization-epoch-check.swift`
