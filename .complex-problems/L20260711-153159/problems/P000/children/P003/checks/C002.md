# Echo 家庭 Context 授权验收通过

## Summary

R002 证明 P003 的 relation、持久化恢复、请求前和回调提交四个边界均已 fail closed，同时 accepted exact-match 保持可用。

## Evidence

- model smoke 直接证明 relation="本人" 仍是 family，explicit self 仍是 personal。
- static gate 验证 acceptedMember、user-scoped context key、unauthorized fallback 与 viewerFamilyMemberID。
- workspace build 验证 optional identity 传播到 gate 与 stale-submit 路径无编译缺口。

## Criteria Map

- relation spoof：resolver 移除 personalRelations。
- persisted context：validatedContext + acceptedMember。
- request preflight：optional echoKnowledgeContextIdentity guard。
- canonical family：repository member digitalHumanId。
- backend recheck：viewerFamilyMemberID。

## Execution Map

- T003/R002 完成 identity、context store、Echo preflight、smokes 与 build。

## Stress Test

- 对话建立 gate 后若关系失效，submit 阶段第二次 optional resolve 会丢弃旧 context。
- 旧全局 UserDefaults context 不再读取，避免跨账号恢复。

## Residual Risk

- 服务端 accepted policy 的完整回归在 P005 统一运行。

## Result IDs

- R002
