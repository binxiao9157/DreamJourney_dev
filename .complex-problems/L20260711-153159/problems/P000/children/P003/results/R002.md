# Echo 家庭 Context 已执行双重授权复核

## Summary

非 self 角色不能再凭 relation 文本获得 personal identity；持久化 context 与每轮 Echo backend context request 都要求当前仓库存在 accepted family member，失效时回退 self 或跳过请求。

## Done

- 移除 relation-only personal inference。
- KBLiteManager 新增 optional authorized persona resolver，family 使用 accepted member canonical digitalHumanId。
- DigitalHumanContextStore 改为 user-scoped persistence，get/set 都复核 accepted member。
- Echo 建 gate 前 preflight，未授权记录 `familyRelationshipUnauthorized` 且不发请求。
- family Context 请求携带 member ID，供后端 accepted policy 复核。
- stale turn submit 再次复核 identity。

## Verification

- knowledge context model smoke 包含 relation spoof 与 explicit self，已通过。
- Echo family context authorization static gate 已通过。
- family authority/repository gates 继续通过。
- xcworkspace Simulator Debug build 通过。

## Known Gaps

- sync/import 仍需 P004 收敛。
- 真机角色切换不在本任务范围。

## Artifacts

- `EchoKnowledgeContextPolicy.swift`
- `KBLiteManager.swift`
- `DigitalHumanContextStore.swift`
- `EchoViewController.swift`
