# 家庭关系 authority 模型已默认拒绝

## Summary

新增统一纯策略与显式 authority source，FamilyMember 的 owner/source/status 组合不再依赖不安全默认值；后端 accepted invitation 是生产唯一放行来源，QA fixture 必须显式开启。

## Done

- 新增 `FamilyRelationshipAuthoritySource`、authorization context/policy。
- FamilyMember 增加 relationship owner/source、context-aware accepted 判断。
- initializer 与 legacy decoder 默认 pending + legacyUnverified。
- backend parser 绑定 ownerUserId/userId 并标记 backendInvitation。
- 新文件已加入 Xcode target。
- 新增并运行纯模型 smoke。

## Verification

- `run-family-relationship-authorization-policy-model-smoke.sh` 通过。
- Xcode workspace Simulator Debug build 通过，证明 MemoryModel 与现有调用方兼容。
- project.pbxproj `plutil -lint` 通过。
- 直接 xcodeproj 构建因 Pods module 解析失败，改用工程权威的 xcworkspace 后通过；不是代码失败。

## Known Gaps

- 现有 FamilyRepository 仍需在 P002 绑定当前 owner/generation，并显式标记 UIQA fixture。
- 当前便捷属性以记录 owner 计算，跨账号隔离由 P002 repository authority boundary 完成。

## Artifacts

- `DreamJourney/Sources/Services/FamilyRelationshipAuthorizationPolicy.swift`
- `DreamJourney/Sources/Services/MemoryModel.swift`
- `Scripts/QA/prd-stitch-ui/run-family-relationship-authorization-policy-model-smoke.sh`
