# 家庭关系 authority 模型验收通过

## Summary

R000 满足 P001：生产放行条件已经从隐式状态字符串升级为 owner/source/status 全维度纯策略，legacy/default/knowledge/QA 均按合同 fail closed，且真实工程编译通过。

## Evidence

- model smoke 直接覆盖 current-owner backend accepted、owner mismatch、pending/failed/revoked、knowledgeCandidate、legacyUnverified 和 QA policy。
- MemoryModel initializer/decoder 已移除 active/accepted 兼容默认。
- fromBackendJSON 显式绑定后端 owner 与 backendInvitation source。
- xcworkspace Simulator Debug build 证明模型字段和 Xcode target 接线兼容现有工程。

## Criteria Map

- owner/source 显式化：FamilyMember 新字段与 policy。
- legacy/default deny：initializer/decoder pending + legacyUnverified。
- backend accepted 放行：纯模型 smoke 正例。
- QA 生产拒绝/显式放行：纯模型 smoke 双向断言。
- 可构建：workspace build succeeded。

## Execution Map

- T001/R000 完成 authority policy、FamilyMember 兼容迁移、Xcode 接线和模型 smoke；没有依赖未执行的子票。

## Stress Test

- 状态比较包含空白和大小写规范化；空 owner 与跨 owner 均拒绝。
- qaFixture 即使 active+accepted 在 production context 仍拒绝。

## Residual Risk

- 仓库账号 generation、候选隔离和显式 QA fixture 属于 P002，不属于本模型子问题。

## Result IDs

- R000
