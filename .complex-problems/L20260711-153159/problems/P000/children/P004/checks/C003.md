# 家庭知识同步与导入授权验收通过

## Summary

R003 满足 P004：远端同步和本地整库分享两条数据出入站旁路均有默认拒绝合同、正反模型证据和真实工程编译证据。

## Evidence

- merge model smoke 证明 exact personal/family identity 可同步，wrong owner/ownerless/wrong persona 被过滤，本地 ownerless 保留。
- static gate 证明 coordinator 没有省略 authorization 的调用，raw graph 不再进入 merge。
- workspace build 证明 KBSync Result API 和 sync engine 签名迁移完整。

## Criteria Map

- 必填 authorization：所有 engine API 与 coordinator calls。
- owner/persona/digital exact：isSyncable guard。
- local legacy only：privateLocal merge 与模型断言。
- release share deny：FamilyKnowledgeSharePolicy。
- UI no false success：Result switch 与 raw rejection。

## Execution Map

- T004/R003 完成 sync engine、coordinator、legacy share、UI、smokes 与 build。

## Stress Test

- family authorization 只同步 family-A，拒绝同 owner personal 实体。
- remote wrong-owner 不合并，local ownerless 不上传也不丢失。
- sourceUserId="unknown" 即使 QA policy 开启仍拒绝。

## Residual Risk

- 没有后端 share grant，所以 release 功能保持关闭；这是安全产品边界而非未实现的隐式 fallback。

## Result IDs

- R003
