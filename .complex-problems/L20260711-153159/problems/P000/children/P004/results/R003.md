# 家庭知识同步与 legacy 分享已默认拒绝未授权数据

## Summary

知识同步现在要求 owner/persona/digitalHuman 精确授权，ownerless legacy 只留本地；未签名整库分享在 release 不生成、不显示、不导入，裸 graph 始终拒绝。

## Done

- 新增必填 `KnowledgeSyncAuthorizationScope` 并覆盖 bootstrap/merge/delta/payload。
- Coordinator 从 authorized current persona 派生 scope，所有生产 sync 调用显式传入。
- wrong owner、ownerless、persona/digital mismatch 从同步和远端合并域过滤。
- local private/legacy 仍保留，不被 remote 删除。
- FamilyKnowledgeSharePolicy 在 release 关闭完整图谱 export/import；unknown/raw graph 拒绝。
- KBSync UI release 隐藏动作，拒绝时显示明确原因。

## Verification

- 扩展后的 knowledge three-way merge model smoke 通过，含 personal/family identity 矩阵。
- knowledge family sync/import static gate 通过。
- xcworkspace Simulator Debug build 通过。

## Known Gaps

- 后续若产品开放整库家庭共享，需要独立后端 grant/签名协议；本任务没有伪造该合同。
- P005 仍需把 gates 接入默认 release regression/QA 并运行全量验证。

## Artifacts

- `KnowledgeThreeWayMerge.swift`
- `KnowledgeSyncCoordinator.swift`
- `KBLiteMultiUser.swift`
- `KBSyncViewController.swift`
