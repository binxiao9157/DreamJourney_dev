# 家庭仓库账号生命周期验收通过

## Summary

R001 满足 P002：知识人物与家庭成员已物理分离，账号 owner/generation、远端权威替换和 owner-scoped overrides 均有代码与构建证据。

## Evidence

- static gate 检查 safe defaults、candidate 集合、generation guard、stale response、owner-scoped keys、无 release seed 和显式 QA fixture。
- workspace build 证明 FamilyRepository API 迁移兼容现有家庭、Echo、时间信件和声音复刻调用。
- authority smoke 证明 repository 复用的底层关系策略正确。

## Criteria Map

- candidate/member 分离：`knowledgeCandidates` 与 candidate-only sync。
- owner/generation：login/logout observers、capturedGeneration。
- remote authority：`replaceRemoteMembers` + `isOwnedBackendRecord`。
- scoped override：`ownerScopedKey`。
- mock 边界：seed 移除、QA source 显式化。

## Execution Map

- T002/R001 完成仓库、QA fixture、静态 gate 与 Simulator build。

## Stress Test

- 空 owner、owner mismatch、generation mismatch 和后端记录数量不一致均拒绝合并。
- 后端返回空数组会清除旧成员，避免增量 merge 留下已失效关系。

## Residual Risk

- 真正 family Context 的使用边界由 P003 验证；本子问题只保证 repository 数据源可信。

## Result IDs

- R001
