# 家庭仓库已绑定账号并隔离知识候选

## Summary

FamilyRepository 不再是混合 mock、KBPerson 与多账号响应的全局列表；它现在绑定当前 owner/generation，只接受匹配 owner 的后端关系，KBPerson 保留为非授权候选。

## Done

- 新增 `FamilyRelationshipCandidate`，KBLite 更新只重建 candidate 集合。
- 移除 `seedMockData` 和 KBPerson -> member 自动追加。
- 登录/登出切换 active owner 与 generation，并清理 members/candidates/overrides。
- refresh/invite 捕获 generation，拒绝旧账号迟到回调。
- 后端列表执行 owner/source 校验和权威替换。
- mode/voice overrides 改为 owner-scoped key，更新仅限 accepted member。
- AppDelegate/Echo QA 家庭 fixture 显式标记 owner/source。

## Verification

- authority model smoke 通过。
- family repository lifecycle static check 通过。
- xcworkspace Simulator Debug build 通过。
- `plutil -lint` 已在 P001 验证新增 target 接线，当前增量不改变工程结构。

## Known Gaps

- Echo identity/context 的 accepted preflight 属于 P003。
- knowledge sync 与 legacy share import 属于 P004。

## Artifacts

- `DreamJourney/Sources/Services/FamilyRepository.swift`
- `Scripts/QA/prd-stitch-ui/family-repository-authorization-lifecycle-check.swift`
