# Task 22 根任务验收

## Summary

R015 与全部已验收子问题证明 P000 成功完成。当前公开业务只信任当前 owner 的后端 accepted family 关系；候选、legacy、错误 owner、撤销关系和迟到异步工作均不能进入家庭 Echo 或远端知识同步。实现、发布门、非真机构建和状态文档形成了闭环证据。

## Evidence

- 默认拒绝授权 policy 与纯模型 smoke 覆盖 owner、authority source、status、QA 和 legacy 组合。
- repository lifecycle/freshness gates 覆盖账号切换、同账号乱序 refresh 和 owner-scoped overrides。
- Echo reconciliation 与 Context authorization gates 覆盖撤权主动回退和禁止未授权 family 请求。
- sync/import/snapshot/epoch gates 覆盖 wrong-owner、无 grant 分享、旧 extraction/governance callback。
- final8 完整发布回归和两种非真机构建通过；独立审查发现的问题已修复复验。

## Criteria Map

- KBPerson 仅候选：满足。
- 只有 backend invitation accepted 可授权：满足。
- 账号、请求与异步生命周期隔离：满足。
- Echo、音色、收件人、关怀和知识同步共享授权边界：满足。
- legacy 分享默认拒绝：满足。
- 发布回归、文档、构建证据：满足。

## Execution Map

- R015 汇总 P001-P015 的模型、仓库、Echo、同步、运行期并发和发布收口成果。

## Stress Test

- 覆盖账号切换、同账号请求乱序、撤权、持久化旧 Context、已排队治理 callback、relation spoof、wrong-owner 和无签名 graph 导入。

## Residual Risk

- 历史数据迁移与未来跨账号签名 grant 是后续独立能力；当前 fail-closed 行为满足本 P0 目标。

## Result IDs

- R015
