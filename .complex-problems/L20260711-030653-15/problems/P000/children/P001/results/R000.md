# 后端 Mutation Proposal Builder 已实现

## Summary

新增纯函数 proposal builder，并将其 additive 接入 `/kb/extract` v2。服务端现在基于权威 snapshot revision 复用 legacy/匹配 persona ID，未命中时生成稳定 SHA-256 ID，继承净化后的来源与隐私，写入 owner/persona/evidence metadata，并把抽取名称关系解析为有效实体 ID。接口只返回 proposal，不修改 snapshot/change feed。

## Done

- 实现稳定 ID、snapshot 去重、metadata、evidence status 和关系解析。
- `/kb/extract` v2 additive 返回 proposal，旧合同和 dryRun 保持兼容。

## Verification

- 新增 `app/services/knowledge_proposal.py` 和 `tests/test_knowledge_proposal.py`。
- 修改 `app/main.py` 与 extraction 兼容测试。
- worker 全量后端测试 209/209 通过。
- 主控复跑 7 项定向测试、`py_compile` 和 `git diff --check` 通过。

## Known Gaps

- 同名不同实体只做保守唯一匹配，歧义留给后续用户治理。
- proposal 提交前 revision 变化仍由既有 Mutation V2 409/三方合并处理。

## Artifacts

- `app/services/knowledge_proposal.py`
- `tests/test_knowledge_proposal.py`
- `app/main.py`
- `tests/test_core_services.py`
