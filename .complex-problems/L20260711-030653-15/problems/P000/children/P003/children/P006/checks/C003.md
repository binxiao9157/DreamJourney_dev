# iOS Identity-bound Proposal 合并与 Persona 本地策略验收

## Summary

P006 成功。核心编译错误已修复，identity 由会话开始到提取、合并和 Echo fallback 一致贯穿，旧合同兼容边界保留。

## Evidence

- generic Simulator workspace `BUILD SUCCEEDED`。
- 五条 knowledge model/static gates 通过。
- source guard 显式检查 envelope、captured/current identity、proposal-first、watermark 和 Echo expected identity。

## Criteria Map

- proposal merge/ID remap：实现并通过 workspace 编译与 relation decode smoke。
- metadata/source/evidence：模型、backend proposal 和 manager 写入均有检查。
- user/persona 竞态：completion 同时校验 generation 与 current identity。
- personal legacy/family strict：纯 policy smoke 通过。
- old extraction/graph：legacy envelope 与 graph decode smoke 通过。

## Execution Map

- R003 完成 iOS identity-bound proposal 消费并由主控修复、复核和构建。

## Stress Test

- family 对缺 metadata legacy entity 拒绝，personal 继续兼容。
- candidate/rejected evidence 不进入本地 generation。
- malformed/identity-mismatch/tombstone proposal 被拒绝，且不推进 backend extraction watermark。

## Residual Risk

- 无真机验收是用户明确边界；非真机代码与合同验收已满足本问题。

## Result IDs

- R003
