# iOS Proposal Schema 与 Client Envelope 已实现

## Summary

iOS 已具备向后兼容的 persona metadata/proposal 模型，并新增 envelope API 发送 persona identity、严格解码 optional proposal；旧服务端无 proposal 时仍返回 legacy extraction。

## Done

- KBLite 四类实体增加 optional owner/persona/evidence/source-turn 字段。
- 新增完整 proposal/upserts/policy/tombstone/envelope Codable 模型。
- 新增 `extractKnowledgeEnvelope`，旧 `extractKnowledge` wrapper 保持编译兼容。
- 显式 malformed/null proposal 会失败，字段缺失才视为 legacy。

## Verification

- proposal model smoke 与既有 context model smoke 均通过。
- worker generic Simulator workspace build 成功。
- runner bash syntax 与 scoped whitespace 通过。

## Known Gaps

- KBLiteManager 尚未消费 envelope，由 P006 接入。

## Artifacts

- `DreamJourney/Sources/Services/KBLiteModels.swift`
- `DreamJourney/Sources/Services/DreamJourneyBackendClient.swift`
- `Scripts/QA/prd-stitch-ui/knowledge-proposal-model-smoke.swift`
- `Scripts/QA/prd-stitch-ui/run-knowledge-proposal-model-smoke.sh`
