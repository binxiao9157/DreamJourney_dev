# iOS Proposal Schema 与 Client Envelope 验收

## Summary

P005 成功。新 schema/client 合同可独立编译，旧图谱与旧服务端兼容，显式坏 proposal 不会静默污染合并路径。

## Evidence

- proposal/context 两个 Swift model smoke 通过。
- CocoaPods workspace generic Simulator Debug build 成功。

## Criteria Map

- 旧 KBLite JSON：smoke 解码通过。
- 完整 proposal：revision、relations、metadata、policy 解码通过。
- legacy envelope：proposal=nil 通过。
- malformed proposal：custom envelope decode 采取失败策略。
- request identity：client payload 包含 personaScope/digitalHumanId。

## Execution Map

- R002 完成 models、client 和 smoke runner。

## Stress Test

- proposal 新实体缺 createdAt/updatedAt 仍可解码。
- mutationProposal 键存在但为 null/malformed 时不当作 legacy。

## Residual Risk

- proposal 合并和异步 identity guard 尚属 P006，不属于本 schema 子问题。

## Result IDs

- R002
