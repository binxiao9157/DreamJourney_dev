# 后端 Persona Context Policy 已实现

## Summary

Context Packet 现在对 KB facts 执行 owner/persona/digitalHuman/evidence 的实体级策略，并让 memory、selected candidates 和 generationContext 共享同一 eligible fact 集合。

## Done

- personal 兼容 legacy/self，过滤显式 owner/scope/digitalHuman mismatch。
- family 仅允许 metadata 完整且目标一致的事实。
- observed/confirmed 可生成；candidate/rejected/superseded 与 low confidence 继续过滤。
- 新增稳定 filtered reason 和独立测试。

## Verification

- 主控复跑 Context persona + Archive API 27 项测试通过。
- worker 定向 27 项通过；py_compile 与 diff check 通过。

## Known Gaps

- 缺 metadata 的旧 family 数据有意不可用，需要后续显式迁移，不能自动推断。

## Artifacts

- `app/services/context_packet.py`
- `tests/test_context_packet_persona_policy.py`
- `tests/test_core_services.py`
