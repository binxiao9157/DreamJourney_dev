# 跨仓知识隐私 gate 与发布结果

## Summary

完成 Task 20 的本地跨仓防回退、release QA、Simulator/generic iPhoneOS 构建、双仓提交和推送。release regression 默认运行 fixture-only 知识隐私 gate，不连接生产数据库，也不会执行 `--apply`。

## Done

- 后端新增 `run-backend-knowledge-privacy-maintenance-smoke.sh`，运行 canonical mutation 与 maintenance 的 8 个定向测试。
- iOS QA 新增 `knowledge-privacy-maintenance-contract-check.swift` 和组合 gate，静态守护 canonicalization、事务/回滚、默认 dry-run、脱敏报告与 release 接线。
- gate 接入 release regression，默认启用；release QA package 检查脚本与状态文档。
- 状态文档记录生产 `dry-run -> apply -> dry-run` 安全顺序和非真机边界。
- 后端提交 `e1f06a8 feat: canonicalize knowledge privacy metadata` 已推送 `origin/main`。
- iOS 提交 `190d65f test: guard knowledge privacy canonicalization` 已推送 `origin/feature/prd-stitch-ui-adaptation`。

## Verification

- 后端 verify：267 tests passed；FastAPI、knowledge delta/V2/evidence smoke 通过。
- 跨仓隐私 gate：8 tests passed。
- Release QA package：通过。
- Release regression：`20260711-task20-knowledge-privacy` 通过。
- Simulator：Archive -> Echo 与 delayed reply notification smoke 通过并保存截图。
- generic iPhoneOS build：通过，bundle ID `com.yxj.dreamjourney.app`。
- 双仓 `git diff --check` 和 push 均通过。

## Known Gaps

- 生产服务器尚未部署后端 `e1f06a8`。
- 生产 Postgres dry-run/apply/dry-run 与线上 mutation sentinel 尚未执行，属于 P005。

## Artifacts

- `tmp/visual-qa/prd-stitch-ui/release-regression/20260711-task20-knowledge-privacy/report.md`
- `docs/superpowers/status/2026-07-11-knowledge-mutation-privacy-canonicalization.md`
