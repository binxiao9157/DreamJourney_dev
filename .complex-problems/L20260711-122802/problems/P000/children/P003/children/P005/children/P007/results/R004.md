# 生产知识隐私清洗与线上合同验收结果

## Summary

生产 apply、post-dry-run 归零和线上 V2 mutation sentinel 全部通过。历史 raw source title 旁路已清洗，新写入 response/change/receipt replay 使用同一 canonical mutation，任务文档已更新。

## Done

- 显式 apply 清洗 4 个 change mutation、2 个 receipt result 和 2 个 V2 receipt payload hash；无 snapshot/change graph 更新。
- apply 后 dry-run 的全部 changed 计数为 0，invalidRecordCount=0。
- 线上合成 QA 用户发送 raw title mutation，再以 canonical title/同 operation ID 重试；重试 duplicate=true、operationPayloadVerified=true。
- 首次响应、receipt duplicate replay 和 change feed mutation 完全一致，raw sentinel 不存在，kind/id 保持。
- sentinel 写入后再次运行生产 dry-run，22 users、22 snapshots、13 changes、8 receipts 全部零待更新。
- 更新 Task 20 验收清单和状态文档，生产 JSON 继续保存在服务器 600 私密目录。

## Verification

- Apply report：mode=apply、status=ok、invalidRecordCount=0。
- Post report：mode=dryRun、status=ok、全部 changed=0。
- Online smoke：firstResponseCanonical、duplicateReceiptReplayCanonical、changeFeedCanonical、rawTitleAbsent、rawCanonicalRetryIdempotent、operationPayloadVerified 均为 true。
- 最终 dry-run：全部 changed=0。

## Known Gaps

- 本任务不物理删除合成 QA 用户；账号只包含合成知识，不含真实用户内容。后续测试数据保留策略属于独立运维治理范围。

## Artifacts

- `/opt/backups/dreamjourney/task20-apply-20260711-144331.json`（服务器私密，600）。
- `/opt/backups/dreamjourney/task20-post-dry-run-20260711-144331.json`（服务器私密，600）。
- `docs/superpowers/status/2026-07-11-knowledge-mutation-privacy-canonicalization.md`。
