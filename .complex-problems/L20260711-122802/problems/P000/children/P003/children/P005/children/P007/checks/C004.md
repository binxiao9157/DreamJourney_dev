# P007 成功检查

## Summary

R004 证明生产历史清洗和新写入 canonical 合同均已完成。apply 前置备份有效，更新量与 pre-dry-run 一致，apply 后及 sentinel 写入后均保持零待清洗项。

## Evidence

- apply 聚合精确更新 preflight 发现的 4/2/2 项，invalidRecordCount=0。
- post-apply 和 sentinel 后两次 dry-run 的所有 changed 值均为 0。
- 线上 smoke 使用 raw title 首次提交、canonical title 同 operation ID 重试，返回 duplicate=true、operationPayloadVerified=true。
- response、receipt replay、change feed mutation 相等，raw sentinel absent，canonical title 为服务端“对话来源”。
- 报告输出只有聚合布尔值/计数，token 和身份标识未输出；服务器 JSON 权限 600。

## Criteria Map

- 显式 apply 成功：成功。
- post-dry-run 归零：成功。
- 线上首次响应/change/receipt replay canonical：成功。
- raw/canonical 幂等重试：成功。
- 文档更新与敏感信息边界：成功。

## Execution Map

- R004：生产 apply、两次归零 dry-run、线上 sentinel、文档更新。

## Stress Test

- sentinel 写入发生在首次归零后，随后再次 dry-run 仍为零，证明新写入不会重新产生历史维护债务。
- 5 秒 lock timeout 保证并发锁竞争不会无限等待；本次 apply 未超时且单事务完成。

## Residual Risk

- 合成 QA 账号仍保留在生产，属于测试数据生命周期治理，不影响本任务的隐私 canonicalization 合同。
