# P006 成功检查

## Summary

R003 满足部署、备份与只读 preflight 的全部条件，且未越界执行生产 apply。真实 Postgres dry-run 无无效记录，备份可验证，服务版本和运行环境明确。

## Evidence

- 服务器 HEAD 与容器代码为 `d6d13be`，包含 maintenance CLI 和 5 秒 lock timeout。
- 公网及容器 health 均证明 production/Postgres。
- full DB custom dump 非空、权限 600、有 SHA-256，存放在 Git 仓库外的 root 私密目录。
- dry-run schema 严格校验通过，invalidRecordCount=0，报告仅含聚合键。
- preflight 只运行 CLI 默认模式，没有传入 `--apply`。

## Criteria Map

- 部署目标版本：成功，安全修订后实际目标为 `d6d13be`。
- 私密备份：成功。
- 真实 Postgres dry-run：成功。
- 发现异常时阻断：本次无异常，apply 前置条件成立。

## Execution Map

- R003：部署、备份、health 与 dry-run 聚合证据。

## Stress Test

- 部署前保留 38 个历史未跟踪 `.env.backup*`，确认没有 tracked dirty 后才 pull，未触碰私密备份。
- API 重启初始连接 reset 后使用有界重试，最终 health 合格。

## Residual Risk

- 生产 apply 和线上新写入合同仍由 P007 单独验收；P006 的成功只授权进入下一关，不代表清洗已完成。
