# 生产部署与 maintenance preflight 结果

## Summary

后端已部署包含锁等待上限的 `d6d13be`，生产 API health 为 production/Postgres。已创建受限全库备份并完成真实 Postgres 默认 dry-run；无无效记录，满足进入显式 apply 的前置条件。

## Done

- 服务器后端从 `dd88f17` 更新到 `e1f06a8`，随后补充 5 秒 lock timeout 并更新至 `d6d13be`。
- API 容器重建成功，maintenance CLI 与 lock timeout 均存在。
- 创建 full Postgres custom-format 备份 `/opt/backups/dreamjourney/task20-pre-maintenance-20260711-144201.dump`，权限 600，大小 71985 bytes，SHA-256 `971db1d39ba8795900b273f94d1ace460bf7bca3e8d901ed96859970e2f095e6`。
- 默认 dry-run 报告保存于服务器私密目录，权限 600；未执行 `--apply`。

## Verification

- Health：status=ok、environment=production、store=postgres。
- Dry-run：schemaVersion=1、mode=dryRun、status=ok、invalidRecordCount=0。
- 扫描聚合：21 users、21 snapshots、12 changes、7 receipts。
- 待规范聚合：4 change mutations、2 receipt results、2 receipt payload hashes；snapshot/change graphs 均为 0。
- 输出 schema 只包含 mode/status/scanned/changed/invalid 聚合字段。

## Known Gaps

- 尚未执行显式 apply、post-apply dry-run 或线上新 mutation sentinel；属于 P007。

## Artifacts

- 服务器私密备份和 pre-dry-run JSON；均位于 `/opt/backups/dreamjourney`，未提交 Git。
- 后端提交 `d6d13be fix: bound knowledge maintenance lock wait` 已推送并部署。
