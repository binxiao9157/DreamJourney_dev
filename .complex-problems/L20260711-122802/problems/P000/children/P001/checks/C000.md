# 新 V2 mutation canonical 隐私合同验收

## Summary

P001 成功。新写入所有持久化和回放表面均由 normalize 后的单一 canonical mutation 驱动。

## Evidence

- raw sentinel 在 response/change/receipt/replay 中均不存在。
- 原输入 title 未被原地修改，kind/id/locator 和局部关系保留。
- raw/canonical title 同 operation ID 重放通过，其他语义变化被拒绝。
- 261 项全量测试通过。

## Criteria Map

- 服务端 title：满足。
- fingerprint 前规范化：满足。
- 各持久化表面一致：满足。
- 幂等与冲突边界：满足。

## Execution Map

- privacy helper 负责单实体深拷贝与 title canonicalization。
- knowledge_store normalize 是所有 V2 store 路径的共同入口。
- 两种 store 的测试证明实际 persistence/replay 行为。

## Stress Test

- 保留 partial relationship ID，证明没有误用完整 graph filter。
- kind/id/body 三类变化逐一验证 payload conflict。
- 全量治理回归暴露旧断言后修正为当前 authoritative title，并重跑通过。

## Residual Risk

- 存量历史仍可能含 raw title，必须完成 P002 后才能部署。

## Result IDs

- R000
