# 新 V2 mutation canonical 隐私合同结果

## Summary

V2 mutation 现在在 normalize 阶段统一 canonicalize source-ref title，因此 payload fingerprint、graph merge、首次响应、change、receipt 和 replay 使用同一 mutation。

## Done

- 新增 `canonicalize_source_ref_titles`，仅替换服务端 title，保留 kind/id、locator、关系和正文。
- `normalize_kb_mutation_v2` 返回 canonical mutation，调用方输入保持不变。
- InMemory/Fake Postgres sentinel 测试覆盖首次响应、change、receipt、replay。
- raw/canonical title 重试保持幂等；kind/id/正文变化仍触发 payload conflict。
- 更新治理测试的旧 title 预期为服务端 canonical 文案。

## Verification

- 2 项新增定向测试通过。
- StoreTests + PostgresStoreTests 共 58 项通过。
- `verify_backend.sh` 通过：261 项测试、compile、FastAPI 和知识 smoke。
- `git diff --check` 通过。

## Known Gaps

- 历史 JSONB 尚未清洗，由 P002 处理。
- 尚未提交、部署，由 P003 处理。

## Artifacts

- `app/services/privacy.py`
- `app/services/knowledge_store.py`
- `tests/test_core_services.py`
- `tests/test_postgres_store.py`
- `tests/test_knowledge_governance.py`
