# Middleware 集成结果

## Summary

已将跨账号授权策略接入现有 auth middleware，并在不改变默认 shadow 的前提下提供 enforce-safe 决策。

## Done

- request body 只解析一次并复用给 ownership claim 与策略评估。
- 已分类路由按策略输出 allowOwner / allowFamily / allowRecipient / deny / defer。
- 合法家庭和时间信件收件人 ownership 标记为 delegated。
- 未分类路由继续使用 Task 9 ownership fallback。
- 所有响应（包括 enforce 403）带 PII-safe policy / decision / reason headers。
- system token、anonymous 和旧 profile mismatch 保持兼容。

## Verification

- `STORE_BACKEND=memory PYTHONPATH=. .venv/bin/python -m unittest tests.test_authorization_policy tests.test_auth_sessions.AuthSessionAPITests`：19 项通过。
- 覆盖 shadow 观察、合法家庭 enforce 放行、伪造 viewer enforce 拒绝、system-only enforce 拒绝。

## Gaps

- 生产仍保持 shadow，未部署或开启 enforce。
- HTTP 进程级 smoke、runtime capability 和 iOS release gate 尚未完成。

## Artifacts

- `app/main.py`
- `tests/test_auth_sessions.py`
