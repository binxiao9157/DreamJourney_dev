# 统一知识文件保护验收

## Summary

R001 满足 P002。所有目标 writer 都通过唯一策略写入，host smoke 证明 atomic replacement 后最终 inode 保留 backup exclusion 和 first-unlock protection；既有 store 行为和两种非真机构建均通过。

## Evidence

- static gate 禁止 graph/sync history/sync stores 继续直接调用裸 `Data.write`。
- model smoke 两次替换同一文件并读取 protection/backup 属性。
- base/pending/outbox 的既有模型 smoke 使用临时目录真实读写成功。
- Simulator 与 generic iPhoneOS workspace build 通过。

## Criteria Map

- 五类 writer 统一策略：满足。
- protection 与 backup exclusion：满足。
- 最终 inode 加固：满足。
- 旧文件 best-effort 加固：满足。
- 路径/envelope/测试兼容：满足。

## Execution Map

- R001 完成 policy、五类接线、旧文件加固和 QA。

## Stress Test

- model smoke 覆盖首次创建与 atomic replacement；三个原 store smoke 覆盖 empty/load/save/remove/冲突相关路径。

## Residual Risk

- 真机锁屏与系统备份验收不在非真机范围，文档未将其误标完成。

## Result IDs

- R001
