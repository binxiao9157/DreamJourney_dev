# 统一知识文件保护实现结果

## Summary

新增统一 `KnowledgeLocalStoragePolicy`，graph、remote base、pending mutation、governance outbox 和 legacy sync history 已统一使用 atomic + first-unlock protection 写入，原子替换后再次加固最终 inode，并排除系统备份。既有文件在读取时 best-effort 加固，路径、文件名和 JSON envelope 不变。

## Done

- 集中实现目录准备、backup exclusion、文件保护和原子写入。
- KBLite graph、sync history、base、pending、outbox 五类 writer 全部接入。
- graph legacy quarantine/corrupted backup 与所有既有加载路径补 best-effort 加固。
- 更新三个 host model runner，保持临时目录注入和原有 store 行为。

## Verification

- `run-knowledge-local-storage-protection-model-smoke.sh` 真实写入/替换并校验属性，通过。
- `run-knowledge-local-storage-protection-check.sh` 通过。
- three-way merge、governance outbox、change-feed pagination model smokes 通过。
- Simulator Debug workspace build 通过：`/tmp/dj-task23-simulator-build.log`。
- generic iPhoneOS no-sign build 通过：`/tmp/dj-task23-iphoneos-build.log`。

## Known Gaps

- 真机锁屏前后 Data Protection 行为和系统备份结果不在本轮非真机验收范围。
- sync history 仍是默认关闭的 legacy 全局历史；本轮保护文件但不重启已关闭的跨账号分享产品能力。

## Artifacts

- `DreamJourney/Sources/Services/KnowledgeLocalStoragePolicy.swift`
- `Scripts/QA/prd-stitch-ui/knowledge-local-storage-protection-model-smoke.swift`
- `Scripts/QA/prd-stitch-ui/knowledge-local-storage-protection-check.swift`
