# Persona 可见检索与发布门验收

## Summary

R003 满足 P003。候选过滤顺序、snapshot scope、排名后二次过滤和默认发布门均由源码 gate 与 final3 直接证明；独立审查指出的并发和提交语义问题已纳入最终代码而非仅记录为风险。

## Evidence

- persona ranking static gate 固定 prefilter-before-search、二次过滤和旧 scope Context reject。
- cache gate 固定账号切换区间内 activation，storage model/static gate固定明确 post-commit outcome。
- final3 从零运行全部默认 gates、两种构建和两个 Simulator smoke。
- 独立审查复核没有发现 P0 或 Xcode/QA 接线阻断。

## Criteria Map

- ranking 前 persona/privacy/evidence 过滤：满足。
- 普通本地搜索保持全账号图：满足。
- semantic/keyword 同一 snapshot：满足。
- 旧账号 Context fail closed：满足。
- 默认 release/QA 接线与完整回归：满足。

## Execution Map

- R003 完成检索顺序、发布门、独立审查修复和最终证据。

## Stress Test

- 覆盖账号切换发生在搜索与最近摘要之间、并发 switch activation 反序、atomic commit 后属性加固失败三类时序。

## Residual Risk

- 真实锁屏/备份行为与检索质量基线属于后续外部/P2 验收，不影响当前代码合同。

## Result IDs

- R003
