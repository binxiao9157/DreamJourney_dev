# 语义缓存隔离验收

## Summary

R000 满足 P001。cache scope、key 和并发边界均由生产代码与纯模型/static gate 直接证明；旧账号或旧 generation 的异步预热不能通过最终写入 guard，同 ID 内容更新不会命中旧向量。

## Evidence

- scope 模型验证不同 owner/generation 不相等且不保存 raw userId。
- key 模型验证 entity kind、owner 与文本变化均改变 key，文本仅保留 fingerprint。
- static gate 禁止全局 warm flag、raw ID key 和无 scope lifecycle。
- Simulator workspace build 证明新源文件已进入 target 并可与完整依赖编译。

## Criteria Map

- owner/generation scope：满足。
- kind/ID/text key：满足。
- 并发读写锁：满足。
- stale warm reject：满足。
- 登录切换和关键词 fallback：满足。

## Execution Map

- R000 完成 cache policy、语义搜索实现、KBLite 生命周期接线和 QA。

## Stress Test

- 模型覆盖相同 ID 在不同 owner、generation、kind、text 下的组合；生产写入在计算前后各验证 active scope。

## Residual Risk

- Persona-visible top-K 顺序是独立检索质量问题，不影响本问题的 embedding cache 隔离结论。

## Result IDs

- R000
