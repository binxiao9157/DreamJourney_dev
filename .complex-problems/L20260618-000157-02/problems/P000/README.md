# P0 persona-scoped archive and echo context

## Problem

Close the Lodestar task represented by `/Users/yxj/Documents/Codex/Video/DreamJourney_dev/docs/plans/task_02_p0-persona-scoped-archive-and-echo-context.md` using recursive problem, ticket, result, and check state.

Task context:

# P0 persona-scoped archive and echo context

## Context

Ensure selected digital-human/persona context scopes archive storage, backend archive calls, and echo prompt context so family/self switching can become real without data bleed

## Scope

- Planned files: To be confirmed before execution.

## Steps

- [ ] Initialize or link recursive ledger for this task.
- [ ] Follow recursive `ledger.py next` until the task problem is closed.
- [ ] Sync recursive checkpoint to `progress.md`.
- [ ] Run task-level verification and record evidence.

## Success Criteria

- Recursive ledger reaches `next_action=none` or the task explicitly records why no ledger is needed.
- Implementation satisfies this task's context and scope.
- Verification evidence is recorded in recursive result/check bodies and summarized in `progress.md`.

## Recursive Ledger

- Ledger ID: pending
- Dashboard: pending


## Success Criteria

- Recursive ledger reaches `next_action=none` or the task explicitly records why no ledger is needed.
- Implementation satisfies this task's context and scope.
- Verification evidence is recorded in recursive result/check bodies and summarized in `progress.md`.
