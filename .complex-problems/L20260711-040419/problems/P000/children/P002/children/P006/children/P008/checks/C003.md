# DeleteSource Endpoint 与 Context 成功检查

## Summary

P008 成功；公开 DELETE 合同保持兼容并新增可审计 cascade，来源撤销后后端 Context 不再选中知识。

## Evidence

- API 测试删除 source-matched archive 后，四类实体均 superseded 且 refs 为空。
- ContextPacketBuilder 输出的 kbFacts/generation text 不含旧 fact。
- 重复 operation 不增加 revision；无引用删除保持 revision。
- 既有 Archive API 24 项全部通过。

## Criteria Map

- canonical action：main 使用 memoryArchiveItem + itemId。
- 响应/兼容：保留旧字段并增加 cascade，旧无 query 调用测试通过。
- Context：明确负向断言通过。
- sealed/missing/revision：组合 store 与 ArchiveAPITests 覆盖。
- change feed：治理测试断言仅新增一个 revision。

## Execution Map

- T006 完成 endpoint 编排、duplicate conflict 保护和 Context/API 回归。

## Stress Test

- 同 operationId 第二次删除已不存在的 archive，仍从 change feed 返回 duplicate，不重复 revision。
- 无 sourceRef 命中的删除不写知识 change。

## Residual Risk

- legacy 非 canonical source ID 不在自动级联范围，已明确记录且不会虚报覆盖。

## Result IDs

- R003
